# Skills

Create one directory per skill. Every skill starts with a `SKILL.md` file and
may include `scripts`, `references`, and `assets` directories.

```text
skills/
  review-code/
    SKILL.md
    scripts/
    references/
```
