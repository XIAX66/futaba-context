# Workflows

A workflow describes a repeatable, multi-step process such as implementing a
feature, investigating a bug, reviewing a pull request, or preparing a release.

Add one Markdown file per workflow. Keep detailed procedures in skills and let
the workflow reference them instead of duplicating their instructions.
