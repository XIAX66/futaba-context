# Hooks

Hooks define automation around lifecycle events such as task start, file edit,
validation, and task completion. Agent adapters decide whether and how a hook
can be represented by the target agent.

Hooks are intentionally empty in the first release.
